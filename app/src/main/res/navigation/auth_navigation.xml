<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/auth_navigation"
    app:startDestination="@id/authActivity">

    <activity
        android:id="@+id/authActivity"
        android:name="com.fcascan.proyectofinal.activities.AuthActivity"
        android:label="activity_auth"
        tools:layout="@layout/activity_auth" />
    <activity
        android:id="@+id/mainActivity"
        android:name="com.fcascan.proyectofinal.activities.MainActivity"
        android:label="MainActivity" />
    <activity
        android:id="@+id/fileReceiverActivity2"
        android:name="com.fcascan.proyectofinal.activities.FileReceiverActivity"
        android:label="activity_file_receiver"
        tools:layout="@layout/activity_file_receiver" />
</navigation>